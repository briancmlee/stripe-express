extends layout

block main
    form#payment-form(action="/submit", method="post")
        .form-row
            label Donation amount
            input(name="donation-amount", type="number")

            label(for="card-element") Credit or debit card
            #card-element

            #card-errors
        button(type="submit") Submit